{
  "name": "tomaker",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd web && pnpm --filter @tomaker/sdk build && pnpm --filter @tomaker/app build",
    "install": "cd web && pnpm install --frozen-lockfile"
  }
}
